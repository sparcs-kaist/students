import React from "react";

import FlexWrapper from "@sparcs-students/web/common/components/FlexWrapper";

const SearchThreeInput = () => <FlexWrapper direction="column" gap={16} />;

export default SearchThreeInput;
