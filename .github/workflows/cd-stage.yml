name: Students Dev Push Stage

permissions:
  contents: read
  packages: write

on:
  push:
    branches: ["dev"]
  workflow_call:
    secrets:
      STAGE_WEBHOOK_ENDPOINT:
        description: "The webhook endpoint to trigger the staging workflow"
        required: true


jobs:
  publish-docker-image:
    name: "Build and Publish Latest Docker Image"
    runs-on: blacksmith-4vcpu-ubuntu-2404
    
    strategy:
      fail-fast: true
      matrix:
        include:
          - dockerfile: ./.docker/dockerfile/web.Dockerfile
            image: ghcr.io/sparcs-kaist/students-stage-web
          - dockerfile: ./.docker/dockerfile/api.Dockerfile
            image: ghcr.io/sparcs-kaist/students-stage-api

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - name: "Log in to Github Container Registry"
        uses: docker/login-action@v2
        with:
          registry: "ghcr.io"
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "Extract Metadata"
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ matrix.image }}
          labels: latest

      - name: Build and push Docker image
        uses: useblacksmith/build-push-action@v2
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-production-image:
    name: "Deploy Latest Staging Docker Image to Server"
    needs: [publish-docker-image]
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: "Image push via webhook"
        uses: distributhor/workflow-webhook@v3
        with:
          webhook_url: ${{ secrets.STAGE_WEBHOOK_ENDPOINT }}

